"use client";
import { CHALLENGES_V3, activeEventV3, type ChallengeV3 } from "./modes-v3";
type Lang="es"|"en";
export function ChallengesHubV3({lang,onStart,onBack}:{lang:Lang;onStart:(challenge:ChallengeV3)=>void;onBack:()=>void}){const event=activeEventV3();return <div className="page-shell challenges-v3"><button className="back" onClick={onBack}>← {lang==="es"?"INICIO":"HOME"}</button><div className="title-row"><div><span className="eyebrow">MSC LAB · ARCADE</span><h1>{lang==="es"?"DESAFÍOS":"CHALLENGES"}</h1><p>{lang==="es"?"Reglas especiales sobre la jugabilidad real. Sin victorias compradas y sin goles regalados.":"Special rules on real gameplay. No purchased wins and no gifted goals."}</p></div><div className="record-badge">{CHALLENGES_V3.length}<small>{lang==="es"?"PRUEBAS":"TRIALS"}</small></div></div>{event&&<section className="v3-live-event"><span>{lang==="es"?"EVENTO ACTIVO":"LIVE EVENT"}</span><h2>{event.name}</h2><p>{event.description}</p><small>{new Date(event.startsAt).toLocaleDateString()} → {new Date(event.endsAt).toLocaleDateString()}</small></section>}<div className="challenge-v3-grid">{CHALLENGES_V3.map((challenge,index)=><article key={challenge.id}><span>{String(index+1).padStart(2,"0")}</span><div><small>{challenge.difficulty} · {Math.ceil(challenge.durationSeconds/60)} MIN</small><h2>{challenge.name}</h2><p>{challenge.description}</p><div className="challenge-rewards"><b>+{challenge.rewardMSC} MSC</b><b>+{challenge.rewardXP} XP</b>{challenge.rewardGems>0&&<b>+{challenge.rewardGems} ◆</b>}</div></div><button onClick={()=>onStart(challenge)}>{lang==="es"?"JUGAR":"PLAY"} →</button></article>)}</div></div>}
